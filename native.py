
def mandelbrot(image_size = 100):
    plane = []
    for row in range(image_size):
        plane.append([])
        for placeholder in range(image_size):
            plane[row].append(0)


    for row in plane:
        for column in row:
            

    return plane
